# Binaries
/bin/
*.exe

# Config with secrets (use config.example.yaml as template).
# docker.config.yaml is no longer needed — docker-compose.yaml overrides the
# container-only fields via env vars.
config/config.yaml

# Go test cache
*.test

# IDE
.idea/
.vscode/
*.swp
